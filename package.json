{
  "name": "tablegen",
  "version": "1.0.0",
  "description": "Table html5 generation",
  "private": true,
  "scripts": {
    "build": "babel src -d dist --minified --no-comments && css-minify -f ./src/tablegen.css -o ./dist/"
  },
  "browserslist": "> 0.25%, not dead",
  "author": "Marcello Pagano",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.19.3",
    "@babel/core": "^7.20.5",
    "@babel/preset-env": "^7.20.2",
    "css-minify": "^2.0.0"
  }
}
